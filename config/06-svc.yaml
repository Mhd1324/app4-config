apiVersion: v1
kind: Service
metadata:
  name: app4-service
  namespace: app4-ns
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: app4
  ports:
    - port: 80
      targetPort: 80
